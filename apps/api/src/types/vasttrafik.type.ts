export type VasttrafikHealth = {
	connected: boolean;
	journeyPlanner: {
		connected: boolean;
	};
	geography: {
		connected: boolean;
	};
	trafficSituations: {
		connected: boolean;
	};
};
